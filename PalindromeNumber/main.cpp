#include <iostream>
using std::cin; using std::cout; using std::endl;

/* Your foo function is to return whether a number is Palindrome Number */

bool foo() {
}

int main(void) {
  unsigned num;
  cin >> num;
  cout << foo(num) << endl;
}
